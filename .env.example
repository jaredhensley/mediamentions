# Server configuration
PORT=3000
HOST=localhost
CORS_ORIGIN=http://localhost:5173

# Authentication (optional - if not set, API is unprotected)
# Generate with: node -e "console.log('mm_' + require('crypto').randomBytes(32).toString('hex'))"
API_KEY=

# Scheduler and provider configuration
SCHEDULE_TIME=03:00
TZ=UTC

# Google Custom Search (required)
GOOGLE_API_KEY=
GOOGLE_CSE_ID=
GOOGLE_REFERER=

# Result limits
MAX_RESULTS_PER_PROVIDER=10

# Logging
LOG_LEVEL=info
