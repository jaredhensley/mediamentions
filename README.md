# mentions

This repository now separates the React client into `client/` so it can live alongside the server package without package.json conflicts.

## Client

```
cd client
npm install
npm run dev
```
